<UserControl x:Class="PixPost.Ui.Dialogs.BaseDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:PixPost.Ui.Dialogs"
  mc:Ignorable="d"
  Background="Transparent">
  <UserControl.Template>
    <ControlTemplate TargetType="{x:Type UserControl}">
      <Border CornerRadius="6"
        Background="Transparent"
        BorderThickness="10">
        <Border.Effect>
          <DropShadowEffect Color="#A7A7A7"
            Direction="260"
            ShadowDepth="5"
            BlurRadius="10"
            Opacity="0.4" />
        </Border.Effect>
        <Border BorderBrush="#dee2e6"
          Background="#F2F2F2"
          BorderThickness="1" CornerRadius="6"
          Padding="6">
          <StackPanel Margin="20 15"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch">
            <DockPanel HorizontalAlignment="Stretch"
              VerticalAlignment="Top" Margin="0 0 0 15">
              <TextBlock Text="{Binding DialogHeading}"
                FontSize="21"
                Foreground="#333"
                HorizontalAlignment="Left"
                VerticalAlignment="Top" />
              <TextBlock FontSize="18"
                FontFamily="{StaticResource Phosphor}"
                Text="&#xE4F6;"
                MouseDown="BaseCloseTextBox_OnMouseDown"
                Foreground="#333"
                Cursor="Hand"
                FontWeight="Bold"
                ToolTip="Close"
                HorizontalAlignment="Right"
                VerticalAlignment="Top" />
            </DockPanel>
            <Grid>
              <ContentPresenter />
            </Grid>
          </StackPanel>
        </Border>
      </Border>
    </ControlTemplate>
  </UserControl.Template>
</UserControl>